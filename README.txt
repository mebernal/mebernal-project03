Project folder:
mebernal-project03/

Brief description of submitted files:

doc/
    -Javadoc

src/stacks/StackList.java
    - An implementation of a stack based on a singly linked list. The head of the stack is stored at the head of the list,
      allowing the stack to grow and shrink in constant time.
src/stacks/Navigator.java
    - Class Navigator provides the navigation feature of class BrowserNavigation.
src/stacks/BrowserNavigation.java
    - Main application which contains main(). Simulates a browser's back and forward buttons by recording links that are visited
      and then keeping a stack of "back" links and a stack of "forward" links.

resources/inputFile03.txt
    - Additional use cases.
resources/RUN.txt
    - console output of BrowserNavigation, Navigator, and StackList.

README.txt
    - description of submitted files.